{
  "name": "@cubus/tools",
  "version": "0.0.0",
  "description": "Real tools (read/edit/write/bash) over the fs and subprocess seams",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cubus/tool-registry": "workspace:*"
  }
}
