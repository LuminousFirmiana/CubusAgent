{
  "name": "@cubus/recipe-coding-agent",
  "version": "0.0.0",
  "description": "Coding Agent Recipe for trusted workspaces",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cubus/agent-recipe": "workspace:*",
    "@cubus/system-prompt": "workspace:*",
    "@cubus/tool-approval": "workspace:*",
    "@cubus/tool-registry": "workspace:*",
    "@cubus/tools": "workspace:*"
  },
  "devDependencies": {
    "@cubus/cordis": "workspace:*"
  }
}
