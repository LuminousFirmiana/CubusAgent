{
  "name": "@cubus/agent-recipe",
  "version": "0.0.0",
  "description": "Typed Agent Host and Recipe composition over Cordis",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cubus/agent-loop": "workspace:*",
    "@cubus/cordis": "workspace:*",
    "@cubus/system-prompt": "workspace:*",
    "@cubus/tool-registry": "workspace:*"
  },
  "devDependencies": {
    "@cubus/llm": "workspace:*",
    "@cubus/session": "workspace:*"
  }
}
